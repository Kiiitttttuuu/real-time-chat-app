const WebSocket = require('Websocket').server;
const http = require('http');

const server = http.createServer((request,response) => {

});

WenSocketServer.on('request',request) => {
    const connection = request.accept(null, request.origin);

    connection.on()
}